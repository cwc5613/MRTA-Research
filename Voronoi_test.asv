%% Voronoi Test
close all;clear all; clc;
x = rand(1,10);
y = rand(1,10);
% [vx,vy] = voronoi(x,y);
% figure;
% plot(x,y,'r+',vx,vy,'b-')
% axis([-1 2 -1 2])
% [v,c] = voronoin([x' y']);
% for ii = 1:length(c)
%     disp(c{ii});
% end
bbox=[-1,1;1,0;0.1,1.732050807568877;-1,0]; 
[V,C,XY]=VoronoiLimit(X(:),Y(:),'bs_ext',bbox,'figure','on');

Error in VoronoiLimit (line 175) 
mvx=vx(2,mix); %missing vx
